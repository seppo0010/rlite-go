language: go

go:
    - 1.3
    - tip

install:
    - |
        git clone https://github.com/seppo0010/rlite.git \
        && pushd rlite \
        && make \
        && sudo make install\
        && popd

notifications:
  email: false
